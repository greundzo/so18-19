Il progetto è strutturato su quattro file, gestore.c student.c shared.he shared.c.
Il file gestore.c contiene la funzione principale del programma, dove vengono creati ed inizializzati i semafori, la memoria condivisa e i processi student. Questi ultimi mediante una chiamata excve al file student.c, contenente invece le chiamate alle funzione relative ai singoli studenti; le chiamate alle funzioni relativi agli student e la formazionedei gruppi. Il file shared.h funge da libreria contenente le strutture dati e i prototipi delle funzioni, mentre il file shared.c contiene il codice. Il programma contiene anche la funzione che si occupa dello spawning dei processi student mediante una chiamata excve al file student.c. All'interno della funzione main viene impostato l'handler per i segnali, viene inizializzato il puntatore al segmento di memoria condivisa tra i processi e vengono creati quattro semafori per i quali gli student competeranno a tempo di esecuzione per ottenere le risorse.

